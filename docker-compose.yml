version: '3.8'

services:
  prism-web:
    build: .
    container_name: prism-web-app
    ports:
      - "3003:3003"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
